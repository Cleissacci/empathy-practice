Circular avatar showing initials (or an image). `lg` renders initials in Cormorant display for profile headers.

```jsx
<Avatar size="md" tone="primary" initials="MJ" />
<Avatar size="lg" src="/photo.jpg" alt="Dr. Reyes" />
```

Tones: `primary` (teal), `accent` (amber), `rose`.
